import 'package:flutter/material.dart';
import 'package:open_fashion/search/widgets/search_widgets.dart';

class Screen extends StatelessWidget {
  const Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Search1(),
    );
  }
}
